// Module
// Os módulos servem para que a gente possa quebrar o nosso código em arquivos distintos para facilitar a organização, manutenção e compartilhamento de funções em comum.

export const areaQuadrado = (l) => {
    return l*l
}

export const numeroAleatorio = () => {
    return Math.random()
}
